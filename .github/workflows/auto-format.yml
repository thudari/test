name: Auto Format

on:
  pull_request:
    paths:
      - '**.tf'
      - '**.tfvars'

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Terraform Format
        run: terraform fmt -recursive
