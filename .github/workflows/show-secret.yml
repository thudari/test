name: Show Secrets
description: Action to display GitHub Actions secrets and environment variables for testing
author: Your Name
runs:
  using: composite
  steps:
    - name: Show Environment Variables
      shell: bash
      run: |
        echo "Displaying environment variables..."
        env
    - name: Show Specific Secret
      shell: bash
      run: |
        echo "Displaying specific GitHub secret..."
        echo "MY_SECRET: $MY_SECRET"
